root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/capabot ./cmd/capabot"
  bin = "./tmp/capabot"
  full_bin = "./tmp/capabot serve"
  include_ext = ["go"]
  exclude_dir = ["tmp", "web", "vendor"]
  delay = 500

[log]
  time = false

[misc]
  clean_on_exit = true
