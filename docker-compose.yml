services:
  backend:
    build: .
    ports:
      - "9090:9090"
    environment:
      GOSTAFF_SERVER_ADDR: ":9090"
    network_mode: host

  frontend:
    build: ./web
    ports:
      - "5173:5173"
    environment:
      API_URL: http://localhost:9090
    depends_on:
      - backend
